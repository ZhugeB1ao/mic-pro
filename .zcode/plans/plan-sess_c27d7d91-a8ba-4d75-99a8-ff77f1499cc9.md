## Refactor Footer: tách `columns` + `bottom` thành `left`, `center`, `right`, `bottom` (4 props JSX node riêng biệt, bottom giữ nguyên riêng biệt như hiện tại.)### 1. Sửa `Footer.jsx` (`src/components/ui/Footer.jsx`)**API mới:**```jsx<Footer  left={leftNode}      // JSX — cột trái (brand + logo + mô tả)  center={centerNode}   // JSX hoặc mảng column configs — các cột link ở giữa  right={rightNode}    // JSX — cột phải (aside subscribe / social icons)  bottom={bottomNode}   // JSX — thanh dưới (copyright + links) = giữ nguyên/>``**Thay đổi:*** Footer component đổi từ nhận `columns` (mảng) + `bottom` sang nhận 4 props: `left`, `center`, `right`, `bottom`. Tất cả đều là **JSX node** (truyền gì cũng được).* Xóa helper functions `FooterColumn`, `FooterLinks`, `FooterBottom` vì không cần nữa — các layout tự render JSX đầy đủ, Footer chỉ đóng vai trò **bố cục grid**.* Grid layout: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-[1fr_auto_1fr]` — left chiếm flex, center auto theo nội dung, right chiếm flex. Dưới md responsive xếp dồn thành cột đơn.* `center` nhận **mảng column configs** (`{ key, title, links }`) cho backward compat, render bằng logic nhẹ bên trong Footer.* `left`, `right`, `bottom` mặc định dùng **default values** (brand logo, aside subscribe + rating, bottom copyright + social).* **AgencyLayout** (`src/components/layout/Agency.jsx`): `<Footer />` — không truyền gì, dùng hết defaults. Hoàn toàn không đổi.* **CharityLayout** (`src/components/layout/Charity.jsx`): chỉ truyền `left` (brand charity mô tả) + `right` (social icons). `center` và `bottom` giữ default. Không cần định nghĩa lại toàn bộ mảng 5 cột + bottom object nữa.### 2. Sửa `AgencyLayout.jsx` (`src/components/layout/Agency.jsx`)- Không thay đổi gì (đã dùng `<Footer />` không truyền props).### 3. Sửa `CharityLayout.jsx` (`src/components/layout/Charity.jsx`)- Xóa biến `columns` (mảng 5 cột) và `bottom` (object) cục bộ.- Chỉ truyền `left` và `right` tùy chỉnh cho Footer:```jsx<Footer  left={<CharityBrandNode />}  right={<SocialIconsNode />}/>```- `center` và `bottom` dùng defaults — nếu muốn tùy thì truyền thêm.### 4. Xóa code không dùng* Xóa `defaultColumns`, `defaultAside`, `defaultBottom` constants cũ trong Footer.jsx (thay bằng default values cho 4 props mới).* Xóa helper functions `FooterColumn`, `FooterLinks`, `FooterBottom` (logic render đơn giản hoá).* Xóa `FooterBottom` component (bottom giờ chỉ render `{bottom}` trực tiếp).